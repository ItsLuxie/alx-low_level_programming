#include "main.h"
#include <string.h>
/**
 * _strcmp - compares two strings
 * @s1: string 1
 * @s2: string 2
 * Return: 0
 */
int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
	/**
	 * if result == 0 they are equal else
	 * if result =less 0 then s1 is greater
	 * else is less
	 */
