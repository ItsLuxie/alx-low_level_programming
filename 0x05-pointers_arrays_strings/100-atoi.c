#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * _atoi - converts a sring to an integer
 * @s: source string
 * Return: 0
 */
int _atoi(char *s)
{
	int atoi(s);

	printf("%d\n", s);
	return (0);
}
