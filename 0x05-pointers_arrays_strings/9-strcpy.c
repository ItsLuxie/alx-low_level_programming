#include <string.h>
#include "main.h"
/**
 * *_strcpy - copues one string to another
 * @dest: destination string
 * @src: source string
 * Return: *dest
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
