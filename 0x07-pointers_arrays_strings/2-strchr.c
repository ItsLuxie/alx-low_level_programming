#include "main.h"
/**
 * _strchr - locates a charcater in a string
 * @s: the string
 * @c: the character
 * Return: s1
 */
char *_strchr(char *s, char c)
{
	while (*s != '\0')
	{
		if (*s == 'c')
		{
			return (s);
		}
	}
	if (*s == 'c')
	{
		return (s);
	}
}
