#include <stdio.h>
/**
 * main - entry point with parameters
 * @argc: argument count
 * @argv: argument vector
 * Return: 0 or 1
 */
int main(unsigned int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
