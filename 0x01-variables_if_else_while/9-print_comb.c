#include <stdio.h>

/**
 *  * main - Entry point
 *   *
 *    * Return: Always 0 (Success)
 */
int main(void)
{
	int n = 0;

	while (x < 10)
	{
		putchar(n + '0');
			if (x < 9)
			{
				putchar(',');
				putchar(' ');
			}
			x++;
	}
	putchar('\n');
	return (0);
}

